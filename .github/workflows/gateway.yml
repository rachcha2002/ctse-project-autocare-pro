name: Gateway CI/CD

on:
  push:
    branches:
      - main
    paths:
      - 'gateway/**'
  pull_request:
    branches:
      - main
    paths:
      - 'gateway/**'

env:
  SERVICE_NAME: gateway
  SERVICE_PATH: gateway
  DOCKER_IMAGE: ${{ secrets.DOCKERHUB_USERNAME }}/autocare-gateway

jobs:
  build-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKER_IMAGE }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./gateway
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # deploy:
  #   name: Deploy to AWS
  #   runs-on: ubuntu-latest
  #   needs: build-push
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Configure AWS credentials
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         aws-region: ${{ secrets.AWS_REGION }}
  #
  #     - name: Deploy to EKS
  #       run: |
  #         # Uncomment in Phase 6 when AWS is ready
  #         # aws eks update-kubeconfig --name autocare-cluster
  #         # kubectl set image deployment/gateway \
  #         #   gateway=${{ env.DOCKER_IMAGE }}:${{ github.sha }}
  #         echo "Deploy step - uncomment in Phase 6 when AWS is ready"
